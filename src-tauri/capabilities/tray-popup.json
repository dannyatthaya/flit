{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray-popup",
  "description": "Capability for the trusted local tray popup window. Updates, autostart and player control go through app commands, so no plugin permissions are needed here.",
  "windows": ["tray-popup"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging"
  ]
}
